#include <cstdlib>
#include "grades.h"
using namespace std;

int main(int argc, char** argv) {
    Grades gradesObject;
    gradesObject.createArray();


    
    cout << gradesObject.a[0];




    //gradesObject.calcWeightedScore(gradesObject.divideScores(9.5,10),12);
    
    delete [] gradesObject.a; // When done, free memory pointed to by a.
    gradesObject.a = NULL; // Clear a to prevent using invalid memory reference.

    return 0;
}

