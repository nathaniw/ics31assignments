#include <cstdlib>
#include <iostream>
#include "LinkedList.h"

using namespace std;

int main(int argc, char** argv) {
    Node* A = NULL;
    Node* Z = NULL;
    
    LinkedList* linkedListObject = new LinkedList();  
    int logins = linkedListObject->loginCount;
    cout <<logins;
    bool listIsEmpty = linkedListObject -> isEmpty(A);

    
    return 0;
}

