#include <cstdlib>
#include <iostream>
#include <sstream>
#include <cmath>
#include "manage_input.h"
using namespace std;

int main() {
    ManageInput manageInputObject;
    manageInputObject.getLatVal(manageInputObject.getLatInput());
    manageInputObject.getLonVal(manageInputObject.getLonInput());
    //cout << latVal;
    //cout << lonVal;
    
    return 0;
}
